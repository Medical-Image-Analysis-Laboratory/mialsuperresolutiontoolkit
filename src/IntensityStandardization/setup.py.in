from distutils.core import setup

setup(name='mialsrtkHistogramNormalization',
      version='1.0',
      package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}' },
      packages=['pymialsrtkHistogramNormalization'])


